'use strict';

/* Strings

TODO: create a few variables(strings):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/
debugger;

const firstName = 'Sam'; // Declaration and initialization of the first name.

const lastName = 'Tee'; // Initialization of the last name.

let fullName = firstName + lastName;
console.log(fullName); // Reading of the value of the fullName variable.
