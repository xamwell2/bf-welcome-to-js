'use strict';

/* Undefined

TODO: create a few variables(undefined):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

const age = Number(prompt('Enter your age')); //Declaration of "age" variable.

let name = prompt('Enter your name'); // Declaration of "name" variable.

confirm('Hi ' + name + ' your age is ' + age + '.');
